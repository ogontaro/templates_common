version: "3"

includes:
  copier: "tasks/copier.tasks.yml"
  npm: "tasks/npm.tasks.yml"
  prisma: "tasks/prisma.tasks.yml"
tasks:
  default:
    desc: show commands
    cmds:
      - task -l --sort none
